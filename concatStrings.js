/**
 * 
 * @param {Array<String>} arr
 */
const concatStringsArrFn = (arr) => arr.join('').substring(1);

/**
 * 
 * @param {Array<String>} arr
 */
function concatStrings(arr) {return arr.join('').substring(1);}

module.exports = {concatStrings, concatStringsArrFn}


